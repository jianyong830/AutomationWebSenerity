webdriver{
    driver = chrome
}

webdriver{
    timeouts{
        implicitlywait = 500

    }
}

drivers{

    windows{
        webdriver.chrome.driver = "";
    }

    mac{
        webdriver.chrome.driver = "";
    }

    linux{
        webdriver.chrome.driver = "";
    }
}